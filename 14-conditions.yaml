- name : gathering facts example
  hosts: local
  connection: local
  tasks:
    - name: Gather facts about the system
      ansible.builtin.debug:
        msg: "Gathering facts about the system.{{ ansible_facts }}"