- name: loops example to intall packages
  hosts: local
  connection: local
  become: true
  tasks:
    - name: Loop through a list of packages and install them
      ansible.builtin.package:
        name: "{{ item }}"
        state: present
      loop:
        - nginx
        - mysql
        - python3-pip
        - htop